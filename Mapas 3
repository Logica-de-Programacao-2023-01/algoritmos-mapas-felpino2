package main

import (
	"fmt"
)

func main() {
	m1 := map[int]int{
		1: 2,
		2: 4,
		3: 6,
	}

	fmt.Println(mapa(m1))
}

func mapa(m1 map[int]int) int {
	m3 := 0

	for _, y := range m1 {
		m3 += y
	}

	return m3
}
